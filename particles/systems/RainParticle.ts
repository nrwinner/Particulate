import { Particle } from "./ParticleSystem";
// import { ParticleVector } from "../Emitter";

export class RainParticle extends Particle {

  // constructor(position: { x: number, y: number }) {
  //   super(position);
  //   this.vector = this.generateNewParticleVector();
  //   this.speed = Math.floor(Math.random() * 13) + 10;
  //   this.size = { w: 1, h: 6 };
  // }

  // tick() {
  //   this.calculateMove();

  //   if (this.position.y > 600) {
  //     this.dead = true;
  //   }
  // }

  // generateCanvasElement(context: CanvasRenderingContext2D) {
  //   context.rect(this.position.x, this.position.y, this.size.w, this.size.h);
  //   context.fillStyle = 'black'
  //   context.fill();
  // }

  // protected generateNewParticleVector(): ParticleVector {
  //   return new ParticleVector(0);
  // }
}